import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  rankingContainer: {
    flex: 1,
    paddingBottom: 100,
  },
  floatingContainer: {
    position: 'absolute',
    bottom: 0,
    left: 0,
    right: 0,
  },
});

export default styles;
