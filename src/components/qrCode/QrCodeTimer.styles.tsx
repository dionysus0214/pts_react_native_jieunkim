import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  timerText: {
    fontSize: 14,
    fontWeight: 500,
    color: '#1C1C1E',
    textAlign: 'center',
    marginTop: 32,
    minHeight: 40,
  },
});

export default styles;
